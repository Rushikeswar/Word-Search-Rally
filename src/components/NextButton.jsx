import React from "react";
export default function GenerateButton({onnextlevel}) {
    {
    {
    return (
        <div id="game-options">
            <button id="nextlevel" type="button"   onClick={onnextlevel}>Next Level</button>
        </div>
    );
    }
    }
}